/**
 * @deprecated This interface has been moved to ../ios-cmdline-tools/simctl.ts
 * Import from there instead for better co-location with implementation
 */

// Re-export from new location for backward compatibility
export type {
  SimCtl,
  AppleDevice,
  AppleDeviceRuntime,
  AppleDeviceType,
} from "../ios-cmdline-tools/SimCtlClient";
