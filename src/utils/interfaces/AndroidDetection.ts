/**
 * @deprecated This interface has been moved to ../android-cmdline-tools/AndroidDetectionService.ts
 * Import from there instead for better co-location with implementation
 */

// Re-export from new location for backward compatibility
export type {
  AndroidDetection,
  AndroidToolsLocation,
  AndroidToolsSource,
  AndroidToolInfo,
} from "../android-cmdline-tools/AndroidDetectionService";
