# Hadolint configuration for Dockerfile linting
# https://github.com/hadolint/hadolint

# Ignore rules
ignored:
  # DL3018: Pin versions in apk add - Alpine package versions change frequently
  - DL3018
  # DL3029: Do not use --platform flag - intentional to enforce x86_64 architecture
  - DL3029
  # DL3059: Multiple consecutive RUN instructions - optimizing for readability
  - DL3059

# Trusted registries for base images
trustedRegistries:
  - docker.io
  - gcr.io
  - ghcr.io

# Override specific rules
override:
  error:
    # Always use COPY instead of ADD
    - DL3020
  warning:
    # Use SHELL to change default shell
    - DL4005
  info:
    # Use JSON notation for CMD and ENTRYPOINT
    - DL3025
  style: []

# Allow inline ignores
inline-ignores: true
